# Chapter 04. Pandas 기초

## 목표
Pandas의 기본 사용법과 Pandas에서 사용하는 주요 데이터 구조를 파악한다.

## 4.1 Pandas 사용하기

### 라이브러리 임포트
Pandas 라이브러리를 불러와 Pandas 기능을 사용할 수 있게 하는 과정이다.
일반적으로 pd라는 별칭으로 사용하며,
데이터 처리와 분석을 위해 사용된다.

'''python
import pandas as pd

## 4.2 시리즈 다루기

### 시리즈 Series
Pandas의 기본 데이터 구조로 데이터와 인덱스로 구성되어 있다.
리스트와 유사한 형태지만,
리스트와 달리 데이터와 함께 인덱스를 저장할 수 있다.

- Series()
- 하나의 컬럼 데이터를 다룰 때 사용된다.

## 데이터프레임 다루기

### 1. 데이터프레임 DataFrame
행과 열로 구성된 2차원 데이터 구조로 엑셀 시트와 유사한 테이블 형태이다.
실제 데이터 분석에서 가장 많이 사용되는 형태이다.

- DataFrame()

### 2. loc / iloc
행과 열을 선택하기 위한 인덱싱 방법이다.

- loc : 인덱스 이름(행의 레이블)을 사용해 데이터에 접근
- iloc : 행의 위치를 사용해 데이터에 접근

## 헷갈렸던 점
- loc는 인덱스 기반, iloc는 위치 기반으로 사용된다.

## 한 줄 요약
데이터 분석을 위한 시리즈와 데이터프레임 구조를 이해
