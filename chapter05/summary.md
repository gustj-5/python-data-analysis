# Chapter 05. Pandas 기능 사용

## 목표
Pandas를 사용해 데이터를 불러와
데이터를 확인하고 조작하는 방법을 이해한다.

## 5.1 데이터 불러오기
다양한 형식의 데이터 파일을 데이터프레임으로 변환하여
Pandas에서 활용할 수 있다.

### 1. CSV 파일
쉼표로 구분된 값들이 저장된 텍스트 파일로
많은 데이터셋이 csv 형식으로 제공된다.

- read_csv()

### 2. 엑셀 파일
하나 이상의 시트로 구성되며, 각 시트에는 행과 열로 이루어진 데이터가 저장된다.
데이터를 저장하고 관리하는 데 널리 사용된다.

- read_excel()

### 3. 다양한 옵션
Pandas에서 제공하는 다양한 옵션을 통해
데이터를 더욱 효율적으로 불러올 수 있다.

- usecols
- header=None

## 5.2 데이터 확인하기
분석 방향을 설정하기 위해
데이터의 구조와 내용을 파악하고 품질을 확인하는 것이 중요하다.

### 1. 초반 데이터 확인
시리즈나 데이터프레임의 상단 일부 데이터를 빠르게 확인할 수 있다.
일반적으로 처음 5개의 행을 확인할 수 있고
정수를 인자로 전달해 출력한 행의 개수를 조절할 수 있다.

- head()

### 2. 후반 데이터 확인하기
하단 일부 데이터를 빠르게 확인해
누락된 데이터나 이상치가 있는지 확인한다.

- tail()

### 3. 통계 요약하기
제공되는 통계 정보를 통해 데이터를 간단히 요약해 한눈에 파악할 수 있다.
📌 주의
- 기본적으로 수치형 데이터만 요약됨
- 범주형 데이터는 include='all' 옵션 필요

- describe()

### 4. 데이터 크기 확인하기
데이터프레임의 크기(행과 열의 개수)를 확인할 때 사용된다.

- shape()

## 5.3 개별 통계치 구하기

### 1. 평균 구하기
기본적으로 데이터 전체의 평균값을 확인할 수 있으며
특정 열을 지정하여 해당 열의 평균을 확인할 수 있다.

- mean()

### 2. 최댓값 구하기
각 열에서 가장 큰 값을 확인할 수 있다.
비수치형 데이터는 알파벳 순서대로 확인할 수 있다.

- max()
 
### 3. 최솟값 구하기
각 열에서 가장 작은 값을 확인할 수 있다.
비수치형 데이터에서는 알파벳 순서대로 가장 앞에 오는 값을 확인할 수 있다.

- min()

### 4. 데이터 모두 더하기
데이터의 각 열의 값을 합산해 반환한다.
📌 활용
- 문자열 데이터는 문자열을 이어붙임
- 불 데이터는 1과 0을 사용해 합산할 수 있음
- numeric_only=True 옵션을 사용해 수치형 데이터만 합산할 수 있

- sum()

## 5.4 객체/메서드/속성 이해하기

### 1. 객체
데이터와 데이터를 사용하는 동작을 포함하는 단위이다.
Pandas에서 사용하는 데이터프레임도 하나의 객체이다.

### 2. 메서드
객체에 속한 함수로 객체의 상태를 변경하거나 특정 작업을 수행한다.

### 3. 속성
객체가 가지고 있는 데이터나 상태를 나타낸다.
메서드는 동작을 수행하거나 결과를 반환하지만,
속성은 단순히 객체의 현재 상태를 나타낸다.

## 헷갈렸던 점
- 메서드는 함수의 결과값을 반환하고 속성은 데이터의 상태를 보여준다.

## 한 줄 요약
Pandas로 데이터를 불러온 뒤,
분석 전에 데이터의 구조와 특성을 점검하는 단계
